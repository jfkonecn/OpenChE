<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lib="clr-namespace:EngineeringMath.Resources;assembly=EngineeringMath"
             x:Class="CheApp.View.FunctionPage"
             Title="{Binding FullName}">
    <ContentPage.Content>
        <TableView Intent="Settings" HasUnevenRows="True">
            <TableRoot>
                <TableSection Title="{x:Static lib:LibraryResources.Settings}">
                    <ViewCell>
                        <ListView ItemsSource="{Binding ActiveSettings}" HasUnevenRows="True">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextCell Text="{Binding Name}" Detail="{Binding SelectedStr}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </ViewCell>
                </TableSection>
                <TableSection Title="{x:Static lib:LibraryResources.Inputs}">
                    <TextCell Text="hello"/>
                </TableSection>
                <TableSection Title="{x:Static lib:LibraryResources.Outputs}">
                    <TextCell Text="hello"/>
                </TableSection>
            </TableRoot>
        </TableView>
    </ContentPage.Content>
</ContentPage>